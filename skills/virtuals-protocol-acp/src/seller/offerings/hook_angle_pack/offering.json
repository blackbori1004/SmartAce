{
  "name": "hook_angle_pack",
  "description": "Instant Hook Pack (single response): get 10 conversion hooks in structured JSON within 60s.",
  "jobFee": 0.01,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "offer_name": { "type": "string" },
      "target_audience": { "type": "string" },
      "goal": { "type": "string", "description": "clicks|starts|replies" },
      "tone": { "type": "string", "description": "bold|trust|educational" }
    },
    "required": ["offer_name", "target_audience", "goal"]
  }
}
