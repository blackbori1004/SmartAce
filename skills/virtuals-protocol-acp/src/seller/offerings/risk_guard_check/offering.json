{
  "name": "risk_guard_check",
  "description": "Pre-trade Risk Guard (under 60s): get max position size, SL/TP bands, and kill-switch rules before execution. Best next step after Quick Risk Score.",
  "jobFee": 0.29,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "market": {
        "type": "string",
        "description": "Trading market or pair (e.g. ETH/USDC)"
      },
      "entry": {
        "type": "number",
        "description": "Planned entry price"
      },
      "leverage": {
        "type": "number",
        "description": "Target leverage (e.g. 3, 5, 10)"
      },
      "max_loss_pct": {
        "type": "number",
        "description": "Maximum tolerated loss percentage"
      }
    },
    "required": ["market", "entry", "leverage", "max_loss_pct"]
  }
}
