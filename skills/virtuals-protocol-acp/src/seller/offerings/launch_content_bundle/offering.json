{
  "name": "launch_content_bundle",
  "description": "Instant Launch Bundle (single response): hooks + rewritten offer + outreach snippets in deploy-ready JSON.",
  "jobFee": 0.49,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "offer_name": { "type": "string" },
      "target_audience": { "type": "string" },
      "launch_goal": { "type": "string" },
      "core_cta": { "type": "string" }
    },
    "required": ["offer_name", "target_audience", "launch_goal", "core_cta"]
  }
}
