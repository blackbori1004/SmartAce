{
  "name": "wallet_risk_audit",
  "description": "Wallet Risk Audit (2-3m). Returns concentration score, stablecoin buffer, volatility exposure, and concrete guardrails.",
  "jobFee": 0.99,
  "jobFeeType": "fixed",
  "requiredFunds": false,
  "requirement": {
    "type": "object",
    "properties": {
      "wallet": {
        "type": "string",
        "description": "Wallet address to audit"
      },
      "chain": {
        "type": "string",
        "description": "Chain name (e.g. base, ethereum, arbitrum)"
      },
      "risk_mode": {
        "type": "string",
        "enum": ["conservative", "balanced", "aggressive"],
        "description": "Risk profile for guardrail recommendations"
      }
    },
    "required": ["wallet", "chain" ]
  }
}
